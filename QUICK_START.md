# 任务分配系统 - 快速部署指南

## 🚀 5分钟快速部署

### 第1步：进入项目目录

```bash
cd /root/workspace/xk/yayuan/simple_distribute
```

### 第2步：一键部署

```bash
sudo bash deploy.sh
```

就这么简单！脚本会自动完成所有配置。

---

## ✅ 部署后验证

### 检查服务状态

```bash
./manage.sh status
```

### 健康检查

```bash
./manage.sh health
```

### 访问系统

- **管理员端**: http://你的IP:8082
- **员工端**: http://你的IP:8082/employee

---

## 🎮 常用管理命令

```bash
# 查看状态
./manage.sh status

# 重启服务
./manage.sh restart

# 查看日志
./manage.sh logs

# 停止服务
./manage.sh stop
```

---

## 🔥 开放防火墙端口（公网访问必须）

```bash
sudo firewall-cmd --permanent --add-port=8082/tcp
sudo firewall-cmd --reload
```

---

## 📝 使用说明

### 管理员端使用

1. 访问 http://你的IP:8082
2. 添加员工（需要钉钉用户ID）
3. 创建任务
4. 分配任务给员工
5. 查看分配记录和员工反馈

### 员工端使用

1. 员工会收到钉钉通知
2. 点击通知链接查看任务
3. 可以接受或拒绝任务
4. 拒绝时需要填写理由

---

## 🐛 常见问题

### Q1: 部署失败怎么办？

查看错误日志：
```bash
./manage.sh logs-error
```

### Q2: 无法访问页面

检查：
1. 防火墙是否开放端口
2. Nginx是否正常运行：`sudo systemctl status nginx`
3. 应用是否正常运行：`./manage.sh status`

### Q3: 如何修改端口？

修改以下文件：
- `gunicorn_config.py` - bind参数
- `nginx_config.conf` - listen和proxy_pass参数

然后重新部署：
```bash
sudo bash deploy.sh
```

---

## 📊 系统架构

```
浏览器 → Nginx(8082) → Gunicorn(8002) → Flask → SQLite
                              ↓
                          Supervisor (进程管理)
```

---

## 🎯 端口说明

| 端口 | 服务 | 访问方式 |
|------|------|---------|
| 8002 | Flask应用 | 仅内部访问 |
| 8082 | Nginx代理 | 外部访问 |

---

## 📞 需要帮助？

运行健康检查：
```bash
./manage.sh health
```

查看完整文档：
```bash
cat README.md
```

---

**就是这么简单！🎉**

